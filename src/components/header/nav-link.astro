---
export type Props = {
  href: string;
};

const { href } = Astro.props;

const isActive = Astro.url.pathname.startsWith(href);
---

<a
  href={href}
  class:list={[
    "py-4 font-500 md:px-4",
    isActive
      ? "text-sky-700 dark:text-sky-300"
      : "transition-colors hover:text-sky-700 dark:hover:text-sky-300",
  ]}
>
  <slot />
</a>
