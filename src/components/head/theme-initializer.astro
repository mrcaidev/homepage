<script is:inline>
  const localTheme = localStorage.getItem("theme");
  const isDarkOs = matchMedia("(prefers-color-scheme: dark)").matches;
  if (localTheme === "dark" || (!localTheme && isDarkOs)) {
    document.documentElement.classList.add("dark");
  }
</script>
